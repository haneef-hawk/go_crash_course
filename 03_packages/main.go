package main // contains main function
import (
	"fmt"
	"math"
)

func main() { // runs by default, no need for function call
	fmt.Println("hello world!")
	fmt.Println("Square root of 8 is ", math.Sqrt(8))
	//fmt.Println("using mathutils ", adding(4, 5))
}
